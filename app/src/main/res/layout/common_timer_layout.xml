<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal">

    <com.github.pavlospt.CircleView
        android:id="@+id/timer"
        android:layout_width="@dimen/circleView_size"
        android:layout_height="@dimen/circleView_size"
        android:layout_margin="@dimen/circleView_margin"
        android:layout_centerHorizontal="true"
        app:backgroundColorValue="#00FFFFFF"
        app:fillColor="#00FFFFFF"
        app:strokeColorValue="@color/colorPrimary"
        app:strokeWidthSize="10"
        app:subtitleText=""
        app:titleColor="@color/black"
        app:titleSize="@dimen/circleView_title_size"
        app:titleText="START" />

    <ListView
        android:id="@+id/listLocalSession"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/timer"/>

    <com.melnykov.fab.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_margin="16dp"
        android:src="@drawable/ic_plus_white_36dp"
        app:fab_colorNormal="?attr/colorPrimary"
        app:fab_colorPressed="@color/white" />
</RelativeLayout>